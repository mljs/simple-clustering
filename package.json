{
  "name": "ml-simple-clustering",
  "version": "0.1.0",
  "description": "Extracts the isolated sub-blocs of matrix",
  "keywords": [
    "clustering"
  ],
  "author": "Andres Castillo",
  "repository": "ml/simple-clustering",
  "bugs": {
    "url": "https://github.com/ml/simple-clustering/issues"
  },
  "homepage": "https://github.com/ml/simple-clustering",
  "license": "MIT",
  "main": "./src/index.js",
  "directories": {
    "lib": "src",
    "test": "test"
  },
  "scripts": {
    "test": "mocha --require should --reporter mocha-better-spec-reporter --recursive",
    "build": "cheminfo build --no-uglify"
  },
  "devDependencies": {
    "mocha-better-spec-reporter": "^3.0.1",
    "mocha": "^2.4.5",
    "should": "^8.2.2",
    "cheminfo-tools": "1.3.4"
  },
  "dependencies": {
  }
}
